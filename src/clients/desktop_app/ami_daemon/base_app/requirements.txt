# Core dependencies
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# CLI framework
click>=8.0.0
rich>=13.0.0

# HTTP client
requests>=2.31.0
httpx>=0.24.0

# Memory and vector store
mem0ai>=0.1.0
chromadb>=0.4.0

# LLM providers
openai>=1.0.0
anthropic>=0.25.0

# Browser automation (optional)
browser-use>=0.1.0

# System monitoring
psutil>=5.9.0

# Database
aiosqlite>=0.19.0
sqlite3  # 标准库，通常已包含

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Additional dependencies for session storage
pathlib  # 标准库，通常已包含
json  # 标准库，通常已包含
logging  # 标准库，通常已包含
uuid  # 标准库，通常已包含
datetime  # 标准库，通常已包含