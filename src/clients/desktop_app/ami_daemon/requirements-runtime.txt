# Runtime dependencies for App Backend daemon
# Only includes packages actually needed for running the daemon

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# HTTP client
httpx>=0.25.0

# Configuration
pyyaml>=6.0.0

# WebSocket
websockets>=12.0

# Browser automation (using eigent_browser)
playwright>=1.40.0

# System monitoring
psutil>=5.9.0

# Database
aiosqlite>=0.19.0

# LLM providers
anthropic>=0.25.0
openai>=1.0.0

# JSON parsing
json-repair>=0.30.0

# Document formatting (Markdown → DOCX/PDF)
markdown>=3.5.0
htmldocx>=0.0.6

# DO NOT include these (dev/test only):
# - pytest, pytest-asyncio
# - mypy, black, isort
# - jupyter, ipython
# - chromadb (removed, was only used by mem0)
# - mem0ai (removed, never enabled)
# - camel-ai (removed, replaced by AMI Task Executor)
# - browser-use (removed, replaced by eigent_browser)
